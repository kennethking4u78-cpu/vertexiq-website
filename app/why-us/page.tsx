import type { Metadata } from "next";
import { products } from "@/lib/content";
import { createMetadata } from "@/lib/seo";
import { CardLink, Checklist, Container, CTA, PageIntro, SectionHeading } from "@/components/ui";
export const metadata:Metadata=createMetadata("Why VertexIQ","Business-first thinking, product engineering experience and connected systems without unnecessary technology.","/why-us");
export default function Page(){return <><PageIntro eyebrow="Why VertexIQ" title="Technology decisions grounded in the business." description="VertexIQ combines public experience, operational systems and product engineering so the customer journey and the work behind it can be considered together."/><section className="py-20"><Container><div className="grid gap-12 lg:grid-cols-2"><SectionHeading eyebrow="What makes the approach different" title="Build what is useful. Connect what matters."/><div className="card"><Checklist items={["Business-first discovery before technical prescription","Experience building products as well as client systems","Customer-facing and internal workflows considered together","A long-term view of maintainability and improvement","A willingness to avoid technology that does not add enough value"]}/></div></div></Container></section><section className="border-y border-white/8 bg-white/[.02] py-20"><Container><SectionHeading eyebrow="Evidence in the portfolio" title="Platforms with clear ownership and status"/><div className="mt-10 grid gap-5 md:grid-cols-2">{products.map(x=><CardLink key={x.id} href={`/products/${x.slug}`} eyebrow={x.status} title={x.name} description={x.summary}/>)}</div></Container></section><CTA/></>}
